"""Synthetic mock-data generation for the PPL perf/load/use-case suite.

Canonical distributions live in `distributions.py` (stdlib-only, importable);
`schemas/distributions.yaml` is the human-readable mirror + provenance.
"""
