"""PPL perf/load/use-case runner + query catalogue.

Perf pillar (P1) today: per-query latency isolation with warmup + repetitions,
p50/p95/max, latency verdicts, optional correctness vs mock_data.expected, and
cluster-metric snapshots. Load (L*) and use-case (U*) pillars build on this.
"""
